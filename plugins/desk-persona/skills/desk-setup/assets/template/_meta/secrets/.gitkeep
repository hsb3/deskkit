# The one never-committed secrets / live-ops home (K10, K29).
# This .gitkeep is tracked so the folder exists; everything else here is git-excluded.
# Put secret VALUES only here or in environment variables — never in tracked files,
# memory, or communications. On a desk this folder is git-excluded yet still cloud-synced.
