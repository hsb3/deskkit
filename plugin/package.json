{
  "name": "@desk-standard/plugin",
  "version": "0.7.0",
  "private": true,
  "description": "desk-standard plugin: harness-pure core domain library + stdio MCP server over the D7 data-surface loader.",
  "type": "module",
  "exports": {
    "./core": "./core/index.ts",
    "./mcp": "./mcp/server.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "bun test core mcp opencode desk-pm",
    "check:purity": "node scripts/check-core-purity.mjs",
    "check:neutrality": "node ../scripts/check-neutrality.mjs",
    "mcp": "bun mcp/server.ts",
    "package": "mkdir -p claude-plugin/mcp claude-plugin/schema && bun build mcp/server.ts --target=node --outfile claude-plugin/mcp/server.js && cp ../schema/profile.schema.yaml claude-plugin/schema/profile.schema.yaml"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
